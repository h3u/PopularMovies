<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="@string/pref_sortby_key"
        android:title="@string/pref_sortby_label"
        android:defaultValue="@string/pref_sortby_value_default"
        android:entries="@array/pref_sortby_entries"
        android:entryValues="@array/pref_sortby_entry_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
